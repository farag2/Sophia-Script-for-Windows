<Window x:Class="W10SS_GUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"        
        xmlns:local="clr-namespace:W10SS_GUI"        
        xmlns:Controls="clr-namespace:W10SS_GUI.Controls"
        mc:Ignorable="d"
        Name="Window" Title="Windows 10 Setup Script" MinHeight="863" MinWidth="800" Height="863" Width="800" 
        FontSize="18" TextOptions.TextFormattingMode="Display" 
        WindowStartupLocation="CenterScreen" SnapsToDevicePixels="True" 
        ResizeMode="CanResize" ShowInTaskbar="True"
        Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"
        Foreground="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}" MouseLeftButtonDown="Window_MouseLeftButtonDown" Initialized="Window_Initialized" SizeToContent="WidthAndHeight">
    
    <Grid Name="gridWindow">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>            
        </Grid.RowDefinitions>

        <!--#region Header -->        
        <StackPanel Name="windowHeader" Grid.Row="0" Orientation="Horizontal" 
                    Background="{DynamicResource ResourceKey=windowHeaderBackground}">
            <Button Name="buttonHamburger" Template="{StaticResource ResourceKey=buttonHamburger}" Grid.Row="0"
                    Content="&#xE700;"/>            
        </StackPanel>
        <!--#endregion Header -->

        <!--#region Context -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>                
            </Grid.ColumnDefinitions>
            
            <!--#region Hamburger Panel-->
            <Canvas Name="panelHamburger" Grid.Column="0" MinWidth="{StaticResource panelHamburgerMinWidth}" 
                    Width="{StaticResource panelHamburgerMaxWidth}" MaxWidth="{StaticResource panelHamburgerMaxWidth}" 
                    Background="{DynamicResource buttonHamburgerBackground}" >
                <Controls:HamburgerCategoryButton IconText="&#xE1DE;" Text="Privacy &amp; Telemetry"
                                                  Width="{Binding ElementName=panelHamburger, Path=Width}"/>
                <!--<Button Name="buttonCategoryPrivacy" Template="{StaticResource ResourceKey=buttonHamburgerCategory}" 
                        Content="&#xE1DE;"/>-->
                
            </Canvas>
            <!--#endregion Hamburger Panel-->

        </Grid>
        <!--#endregion Context -->        
    </Grid>
</Window>
